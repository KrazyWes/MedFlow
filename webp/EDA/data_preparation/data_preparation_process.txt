MedFlow — CRISP-DM — Data Preparation Process Visualization (TXT)
Source: CRISP-DM/data_preparation.py

===============================================================================
ASCII FLOW (high-level)
===============================================================================

START
  |
  v
Set paths + extensions
  - project_root
  - output_dir = this_datasets/
  - supported: .csv, .xlsx, .xls
  |
  +---------------------------+
  |                           |
  v                           v
DOH pipeline                  PhilGEPS pipeline
Input: raw_datasets/DOH/      Input: raw_datasets/PhilGEPS/
  |                             |
  v                             v
Discover supported files       Discover supported files
  |                             |
  +--> If none: Skip            +--> If none: Skip
  |                             |
  v                             v
For each file:                 For each file:
  - If CSV: load_csv              - If CSV: load_csv
  - If Excel: load_excel          - If Excel: load_excel (all sheets)
  |                             |
  v                             v
Data Understanding snapshots    Data Understanding snapshots
  - raw loaded                   - raw loaded (per file/sheet)
  |                             |
  v                             v
DOH clean: clean_doh_dataframe  Merge all loaded DataFrames
  - normalize headers            |
  - strip peso/currency chars    v
  - convert UNIT COST/TOTAL      Data Understanding snapshot
  |                               - merged (pre-filter / pre-impute)
  v                             |
Add Year (CSV or sheet)         v
  |                             Filter medical records by:
  v                               "UNSPSC Description" contains keywords
Merge all DOH frames            |
  |                             v
  v                             Data Understanding snapshot
Data Understanding snapshot       - filtered (pre-impute)
  - merged (pre-impute)         |
  |                             v
  v                             Missing values: handle_missing_values_philgeps
Missing values: handle_missing_values_doh
  - compute derived totals/      - numeric -> median (or 0)
    unit cost/quantity           - categorical -> mode or 'N/A'
  - numeric -> median (or 0)     |
  - categorical -> 'N/A'         v
  |                             Data Understanding snapshot
  v                               - filtered (post-impute)
Data Understanding snapshot      |
  - merged (post-impute)         v
  |                             Save CSV to this_datasets/
  v
Save CSV to this_datasets/
  - doh_medicine_distribution_2022_2025.csv
  |
  v
END ("Data preparation complete.")

